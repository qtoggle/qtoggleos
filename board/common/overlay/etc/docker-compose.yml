services:
  qtoggleserver:
    image: qtoggle/qtoggleserver:${QTOGGLESERVER_VERSION}
    hostname: qtoggleserver
    restart: unless-stopped
    network_mode: host
    uts: host
    privileged: true
    volumes:
      - /data/qtoggleserver:/data
      - /:/host
